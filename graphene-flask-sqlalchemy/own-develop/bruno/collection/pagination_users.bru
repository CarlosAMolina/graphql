meta {
  name: pagination_users
  type: graphql
  seq: 9
}

post {
  url: {{protocol}}://{{domain}}:{{port}}/{{path}}
  body: graphql
  auth: none
}

body:graphql {
  query {
    paginationUsers {
      totalCount
    }
  }
}

assert {
  res.status: eq 200
  res.body.data.aggregateUsers.totalCount: eq 4
}
