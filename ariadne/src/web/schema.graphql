type Person {
    name: String!
    age: Int
}

input PeopleFilter{
    maxAge: Int
    minAge: Int
}

type Query {
    people(input: PeopleFilter): [Person!]!
    maxAgePeople: Int
}

schema {
    query: Query,
}
