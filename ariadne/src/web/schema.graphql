type Person {
    name: String!
    age: Int
}

input PeopleFilter{
    maxAge: Int
    minAge: Int
}

enum AggregateFunction {
    max
    min
}

type Query {
    people(input: PeopleFilter): [Person!]!
    aggregatePeople(function: AggregateFunction!, field: String!): Int
}

schema {
    query: Query,
}
